{
  "name": "wago-store",
  "version": "1.0.0",
  "description": "Payment Gateway Sederhana",
  "main": "api/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongoose": "^8.0.0",
    "qrcode": "^1.5.3"
  }
}
